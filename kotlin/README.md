# Kotlin reference module

This is a dependency-free Kotlin/JVM reference for DTO shape, canonical JSON subset, state reducer, batch ledger and Mock Controller. It is intentionally not an Android APK yet; the Android/AIDL shell is Gate A after Gate 0 acceptance.
