[project]
name = "a620-gate0"
version = "0.1.0"
description = "A620 Gate 0 reference implementation"
requires-python = ">=3.12"
dependencies = ["jsonschema>=4.20", "cryptography>=45"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
